# HERMES B01-R1 缺陷审查与修复验收报告 (R01 - R10)

审查依据：《HERMES_B01_审查与修复执行包_2026-09-07.md》R01–R10 与《HERMES_Gemini_执行包_P0_2026-09-07.md》  
执行日期：2026-09-08  
执行环境：macOS, PostgreSQL 17 (独立端口 5433), Node.js v22, Next.js 15.5.25  
测试数据库：`postgresql://hermes_app:***@localhost:5433/hermes_next_test?schema=public` (严格保护开发库 `hermes_next_dev`)  
验收套件：`npm run test:r1` (`tests/acceptance-b01-r1.test.ts`)  
验收结论：**10 / 10 缺陷项全部修复并通过动态测试 (100% PASS)**

---

## 1. 逐项审查与修复执行明细 (R01 - R10)

| 缺陷 ID | 优先级 | 审查发现代码缺陷 | 修复实施与实现位置 | 动态测试与验证证据 |
| :--- | :--- | :--- | :--- | :--- |
| **R01** | P1 | `session.ts:48` 将 Cookie 用户 ID 当身份，无签名或服务端会话凭证校验；生产分支可能伪造凭证 | 建立基于数据库 `Session` 表的服务端会话机制，SHA-256 哈希存储 token；生产环境误用开发模拟头时严密 fail-fast 抛 403 (`src/modules/identity/session.ts`) | **PASS**：篡改 Token 拦截 (401)、登出后注销拦截 (401)、生产环境传入模拟头阻断 (403) |
| **R02** | P1 | 首页与详情页未经验证全量读取项目、用户、产品，无成员与租户过滤 | 页面与 API 统一收敛至同一套授权服务，严格按 `organizationId` 与项目成员关系过滤，用户选择下拉列表限制在同租户授权范围 (`src/modules/projects/service.ts`, `src/app/page.tsx`, `src/app/projects/[id]/page.tsx`) | **PASS**：跨租户跨公司非法读取被强力阻断抛出 404/403 |
| **R03** | P1 | 证据写入未校验项目成员，客户端可伪造 `hash` 或 `VERIFIED` | 仅 PM/负责人可录入证据，服务端自动计算并校验 SHA-256 哈希，初始为 `UNVERIFIED`，需独立核实接口且留存核实人/时间流水 (`src/modules/evidence/service.ts`, `/api/evidences/[id]/verify`, `/api/attachments/[id]`) | **PASS**：普通观察员写入被拒 (403)，伪造哈希被覆盖，独立核实后留存人与时间戳 |
| **R04** | P1 | 决策包提交无状态前置条件守卫，已批准包可被重新设为 `IN_REVIEW` 并覆盖快照 | 状态机前置条件限制仅 `DRAFT` 或 `CHANGES_REQUESTED` 允许提交；提交时生成冻结快照 `snapshot`，已批准包禁止重复提交覆盖 (`src/modules/decisions/service.ts`) | **PASS**：重复提交已批准包被拦截并抛出 `ConflictError (409)`，快照未被覆盖 |
| **R05** | P1 | 状态检查在事务外，更新按单一 ID 执行无并发防竞态，跨表写入不原子 | 将前置校验、条件更新 (`status: IN_REVIEW`)、阶段推进、唯一打样准备任务生成与审计纳入单个 `$transaction` 事务原子操作 (`src/modules/decisions/service.ts`) | **PASS**：真实双并发裁决竞争，严格互斥：一个成功推进，另一竞争者抛出 409 冲突 |
| **R06** | P1 | `scopeHash` 仅检查包内声明，未读取底层真实产品/成果/证据；项目基线修改后旧批准未失效 | 建立权威基准校验与 `scopeHash` 动态核算；项目目标/技术约束/产品版本/成果证据发生任何漂移，旧批准即刻被标为失效待复核；更新项目使用 `expectedRevision` 乐观锁 (`src/modules/projects/service.ts`) | **PASS**：刚批准时展示有效批准；项目目标一旦修改，`hasValidApproval` 立即变为 `false` 并输出待复核警告 |
| **R07** | P1 | 批准未严格检查真实/演示性质，以标题模糊匹配 DEMO；固定产品被研发门推回打样 | 采用实体正式字段 `isDemo` 与 `EvidenceNature.DEMO/REAL`；禁止 DEMO 证据进入真实项目打样门；禁止固定产品执行研发打样门 (`src/modules/decisions/service.ts`) | **PASS**：引用 DEMO 证据批准真实项目被 422 阻断；固定产品尝试执行打样门被 422 阻断 |
| **R08** | P1 | 固定产品项目未校验产品版本所属公司，未持久化版本关联 | 建立产品创建、不可变发布与版本绑定完整服务，固定产品强制外键持久关联 `productVersionId` 并校验租户同属 (`src/modules/products/service.ts`, `prisma/schema.prisma`) | **PASS**：跨租户版本引用被拦截 (422)，固定产品创建成功后持久持有版本外键，阶段为 `PRODUCTION_PREP` |
| **R09** | P1 | 任务输入版本未随需求更新失效，审查操作会更新所有历史成果 | 引入 `WorkSubmission` 批次与当前成果指针，审查仅针对当前提交批次，历史退回批次完整归档，失败回执不降级已验收任务 (`src/modules/work/service.ts`) | **PASS**：批次 1 退回状态保持 `REJECTED`，批次 2 独立为 `PENDING` 并验收通过；迟到失败回执未使已验收任务退回 |
| **R10** | P1 | 测试脚本直接 TRUNCATE 业务库，无目标保护，声称通过但缺乏真实测试 | 设立 `tests/test-safety.ts` 防护守卫，端口限定 5433 且库名必须包含 `test`，开发库与生产库执行时强行中断；全套采用真实 HTTP/DB 并发测试 (`tests/acceptance-b01-r1.test.ts`) | **PASS**：测试套件在 `hermes_next_test` 独立运行，开发库零篡改，全部用例绿灯通过 |

---

## 2. 验收执行日志输出摘要

```text
▶ 正在验证 [R10] 测试库物理隔离安全防线...
  ✔ [R10] 守卫通过: 当前连接库为 hermes_next_test (端口 5433)，开发库与生产库受到硬隔离保护！

▶ 正在验证 [R01] 服务端可撤销 Session 机制与生产模拟防线...
  ✔ [R01] 通过: 未签名篡改 Token 被拒 (401)，有效服务端会话成功识别！
  ✔ [R01] 通过: 登出会话注销即时生效 (401)，生产模式模拟头被硬阻断 (403)！

▶ 正在验证 [R02] 严格租户数据隔离与成员跨公司越权防线...
  ✔ [R02] 通过: 跨租户跨公司非法越权均被完全阻断 (404/403)！

▶ 正在验证 [R03] 服务端哈希计算、初始 UNVERIFIED 状态及独立核实流水...
  ✔ [R03] 通过: 证据哈希由服务端计算，核实人与时间独立留痕！

▶ 正在验证 [R04] 决策快照状态机条件写入，已批准包禁止重置覆盖...
  ✔ [R04] 通过: 状态机前置条件守卫有效，已批准决策快照受到完全保护，拒绝覆盖 (409)！

▶ 正在验证 [R05] 并发竞争控制：双并发裁决仅一个成功，另一抛出409冲突...
  ✔ [R05] 通过: 并发竞争原子拦截，仅一个裁决成功，竞争者明确抛出 409 冲突！

▶ 正在验证 [R06] 当前权威基准与 scopeHash 敏感性...
  ✔ [R06] 通过: 需求/基线变更后，旧批准即刻被标为失效不适用 (待复核)！

▶ 正在验证 [R07] 严格门禁：DEMO 证据阻断 REAL 项目、非打样门项目模式限制...
  ✔ [R07] 通过: DEMO 证据阻断 REAL 项目，固定产品项目阻断研发打样门 (422)！

▶ 正在验证 [R08] 产品版本租户隔离、已确认版本持久绑定外键...
  ✔ [R08] 通过: 固定产品与已确认版本成功绑定持久外键，租户与确认校验全部生效！

▶ 正在验证 [R09] 任务提交批次化：仅审当前批次，保留历史退回成果，已验收不被覆盖...
  ✔ [R09] 通过: 提交批次隔离完全成功，历史退回结果完整归档，已验收状态受到坚实保护！

================================================================================
🏆 B01-R1 验收全绿: R01 至 R10 全部高优先级缺陷 100% 修复并验证通过！
================================================================================
```

---

## 3. 交付物完整性与编译检查

1. **TypeScript 类型校验与编译**：
   - 运行 `npm run build`：Next.js 15.5.25 静态优化与路由编译全绿通过。
2. **迁移文件完整性**：
   - 位于 `hermes-next/prisma/migrations/20260907151000_b01_remediation_r1/migration.sql`，支持从空库一键幂等重放。
3. **安全与防误伤**：
   - 端口 5432 原有 Python 进程完好无损。
   - 开发数据库 `hermes_next_dev` 完整保留，未发生任何清空或截断。
